import React from 'react';
import DateFilter from '../date-filter/DateFilter.component';

const Header = () => {
  return (
    <div className="cm-header-container">
      <h1>Logo</h1>
      <DateFilter />
    </div>
  );
};

export default Header;
